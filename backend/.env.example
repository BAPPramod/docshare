# Copy this to .env and fill the values

# Database connection (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://bappramod@localhost:5432/docshare?schema=public"

# Secret key for JWT signing (replace with a secure random string in real apps)
JWT_SECRET="super-test-secret-123"

# Port for backend server
PORT=3000

# Optional
# NODE_ENV=development
